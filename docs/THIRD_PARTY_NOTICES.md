# 第三方依赖与数据边界

EnzyPGM 的核心实现使用 PyTorch、ESM-2（通过 `fair-esm`）和 `torch-scatter`。这些组件不是本仓库代码的一部分，也没有被复制、镜像或再分发；请从各项目的官方渠道安装并遵守其许可证。

训练和生成需要的 ESM-2 权重、EnzyPock 数据、PDB/SDF 结构和由其生成的缓存/结果均不包含在本仓库。使用者必须自行确认上游数据、模型权重及结构文件的访问和再分发权限。

本仓库仅将作者拥有的 EnzyPGM 代码以 MIT 许可证发布；第三方软件、权重和数据继续受各自条款约束。
